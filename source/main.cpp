#include "Parser.h"
#include "Preprocessor\QueryPreprocessor.h"
#include <string>
#include <iostream>
#include "PKB.h"

int main() {
	/*string fileName = "simple.txt";
	Parser p;
	p.parseInput(fileName);


	QueryPreprocessor qpp;

	vector<string> result;


	string query20 = "stmt s1, s2; Select s1 such that Parent*(s1, s2)";

	result = qpp.start(query20);


	for (int i = 0; i < result.size(); i++)
		cout << result[i] << endl;*/\
	return 0;
}